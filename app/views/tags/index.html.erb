<h1>All Tags</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
    </tr>
  </thead>

  <tbody>
    <% @tags.each do |tag| %>
      <tr>
        <td> <%= link_to tag.name, tag %> </td>
      </tr>
    <% end %>
  </tbody>
</table>